import styled from 'styled-components';

export const Title = styled.h2`
  margin: 0 auto 50px;
  display: block;
  text-align: center;
  font-weight: 700;
  font-size: 30px;
  @media (min-width: 768px) {
    font-size: 50px;
  }
`;
